export type Book = {
  id: string;
  title: string;
  author: string;
  price: number;
  cover: string;
};

export type CartItem = {
  id: string;
  title: string;
  quantity: number;
  price: number;
};